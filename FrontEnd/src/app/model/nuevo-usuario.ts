export class NuevoUsuario {
    // el signo ! es para no inicializar las variables
    nombre!: string;
    nombreUsuario!: string;
    email!: string;
    password!: string;
    authorities!: string[];
}
